# Common modules. Import them by default in all days
import algorithm, math, sequtils, sets, std/setutils, strformat, strutils, sugar, system, tables, options
import fusion/matching
import std/nre except toSeq

export algorithm, math, sequtils, sets, setutils, strformat, strutils, sugar, system, tables, options
export matching
export nre except toSeq
